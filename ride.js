class Ride {
    constructor(driver, passenger, startLocation, endLocation) {
        this.driver = driver;
        this.passenger = passenger;
        this.startLocation = startLocation;
        this.endLocation = endLocation;
    }
}

module.exports = Ride;
